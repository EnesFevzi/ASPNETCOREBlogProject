@using BlogProject1.EntityLayer.Concrete
@model List<WriterTask>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_WriterLayout.cshtml";
}

@*<div class="row ">
    <div class="col-12 grid-margin">
        <div class="card">
            <div class="card-body">
                <h4 class="card-title">Proje Listesi</h4>
                <div class="table-responsive">
                    <table class="table">
                        <thead>
                            <tr>
                                <th>#</th>
                                <th> Görev Adı </th>
                                <th> Açıklama </th>
                                <th> Fiyat </th>
                                <th>Tamamlanma</th>
                                <th> Durum </th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model)
                            {

                                <tr>
                                    <td>@item.WriterTaskID</td>
                                    <td><img src="@item.TaskName" /></td>
                                    <td>@item.TaskDescription</td>
                                    <td>@item.TaskCreateDate</td>
                                    <td>@item.IsApproved</td>
                                    <td>
                                        <a href="/WriterTask/EditWriterTask/@item.WriterTaskID/">
                                            <div class="badge badge-outline-success">Detayları Gör</div>
                                        </a>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>*@

<h1>Yazarın Görevleri</h1>
<table class="table table-bordered">
    <tr>
        <th>#</th>
        <th> Görev Adı </th>
        <th> Açıklama </th>
        <th> Tarih </th>
        <th>Tamamlanma</th>
        <th>Sil</th>
        <th>Düzenle</th>
    </tr>
    @foreach (var item in Model)
    {
        <tr>
            <td>@item.WriterTaskID</td>
            <td>@item.TaskName</td>
            <td>@item.TaskDescription</td>
            <td>@item.TaskCreateDate</td>
@*            <td>@item.IsApproved</td>*@
            <td>

                    @if (item.IsCompleted)
                    {
                    <span class="text-success">Tamamlandı</span>
                    }
                    else
                    {
                    <span class="text-danger">Tamamlanmadı</span>
                    }

            @*<form asp-controller="WriterTask" asp-action="CompleteTask">
                <input type="checkbox" name="WriterTaskID" value="@item.WriterTaskID" />
                <input type="submit" value="Tamamlandı" />
            </form>*@
            

            @*<form asp-controller="WriterTask" asp-action="IsCompleted">
                <input type="checkbox" name="WriterTaskID" value="@item.WriterTaskID" />
                <input type="submit" value="Tamamlandı" />
            </form>
            @if (item.IsApproved)
            {
                <span>Tamamlandı</span>
            }*@
            </td>
            <td> <a href="/WriterTask/DeleteWriterTask/@item.WriterTaskID" class="btn btn-danger">Sil</a></td>
            <td> <a href="/WriterTask/EditWriterTask/@item.WriterTaskID" class="btn btn-warning">Düzenle</a></td>
        </tr>
    }
</table>
<a href="/WriterTask/AddWriterTask/" class="btn btn-primary">Yeni Görev Oluştur</a>